VITE_API_URL=http://localhost:3001
const res = await axios.get(`${import.meta.env.VITE_API_URL}/users?username=${trimmedUsername}`);

await axios.post(`${import.meta.env.VITE_API_URL}/users`, {
  name: trimmedName,
  username: trimmedUsername,
  password,
});